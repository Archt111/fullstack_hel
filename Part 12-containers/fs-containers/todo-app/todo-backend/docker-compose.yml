services:
  app:
    image: todo-backend
    build: .
    ports:
      - 3000:3000
    depends_on:
      - mongo
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
      - MONGO_URL=mongodb://root:example@mongo:27017/the_database?authSource=admin